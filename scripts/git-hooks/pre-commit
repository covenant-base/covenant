#!/usr/bin/env bash
set -eu

if [ "${COVENANT_SKIP_PRECOMMIT_CHECKS:-0}" = "1" ]; then
  exit 0
fi

BASH_BIN="${BASH:-bash}"
ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"

"$BASH_BIN" "$ROOT/scripts/check-forbidden-public-identifiers.sh" --repo
